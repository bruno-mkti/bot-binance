# Bot desenvolvido em Node.js para Compra e Venda de criptomoedas

Como executar:

1. Instele o Node.js
1. Execute o comando "npm install"
2. Ajuste os parâmetros no "index.js"
3. Execute o comando "npm start" ou "node index"
